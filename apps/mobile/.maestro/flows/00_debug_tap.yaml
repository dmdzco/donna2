# Debug: Try different tap methods on submit button
appId: com.donna.caregiver

---

- assertVisible: "Welcome Back"

# Try double-tap on submit (same pattern that fixed landing page)
- tapOn:
    id: "sign-in-submit"
- tapOn:
    id: "sign-in-submit"

# Wait for result
- extendedWaitUntil:
    visible: "Dashboard|About You|Sign In Failed"
    timeout: 20000
