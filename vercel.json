{
  "buildCommand": "cd apps/consumer && npm install && npm run build",
  "outputDirectory": "apps/consumer/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
