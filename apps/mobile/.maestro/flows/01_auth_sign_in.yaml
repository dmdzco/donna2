# Flow 01: Sign In with Clerk
# Verifies: Sign In → Dashboard navigation

appId: com.donna.caregiver

env:
  TEST_EMAIL: "maestro-test@getdonna.ai"
  TEST_PASSWORD: "MaestroTest2026"

---

- runFlow: ../subflows/sign_in.yaml

# After sign-in, we land on dashboard (if senior exists) or onboarding (if not)
# Check for either destination
- assertVisible: "Dashboard|About You"
